# LOL-v1 our485 training split

The full `our485/high` and `our485/low` image folders are omitted from git because they are large dataset files.

This project uses the fixed split files in `splits/` to record the train/validation partition.
