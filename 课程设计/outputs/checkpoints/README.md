# Model checkpoints

Trained `.pt` checkpoint files are omitted from git because they are large generated artifacts.

The code, configs, metrics, result images, and exported reports are kept in the repository.
